$(function(){
    // include
    
    $('header').load('inc.html header article') ;
    $('footer').load('inc.html footer div');

    // function init(){

    //헤시정보를 이용한 페이지 처리 
    /*  
        var url = location.hash;
        var pageNum = 0;
        switch(url){
            case '#0' : pageNum=0; break;
            case '#1' : pageNum=1; break;
            case '#2' : pageNum=2; break;
        }
    */
    // $('nav a').on('click',function(){
    //     localStorage.pageNum = $(this).index();
    // });

    // $('nav a').eq(localStorage.pageNum).css({ color:'#f00' });

    
    });     
        
